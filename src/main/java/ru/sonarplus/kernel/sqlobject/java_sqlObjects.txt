Правки DSP, портируемые в Java:

ревизия DSP. 	задача, по котороой в DSP внесена правка

13031		42630	 - исправлено получение значения RecId у объекта TValueRecID 		-	в Java нужно портировать объект TValueRecId

  в связи с отсутствием в java типа Variant придётся подумать, как это сделать.
  Игорь предложил в качестве замены использовать Object, в связи с тем, что "в java всё есть объект".
  Да, наверное так и придётся, но всё равно нужно будет думать.


13198		42927	 * удалены "удобные" конструкторы sql-объектов

  в Delphi от них избавлялись по причине утечек памяти, при исключениях в конструкторах.
  в Java такой проблемы нет, в связи с этим быть может и конструкторы можно оставить?

  для реализации в Java - задача 43961.


13242		43161	 * рефакторинг трансляции кодификаторных запросов

  ещё не анализировал отличия.


13795		43763	 * Изменены выводимые на экран и в протокол сообщения при возникновении ошибок дистилляции запроса.
		 * При ошибке дистилляции в протокол записывается текст частично дистиллированного запроса.

  задача, наверное, не первоочередная. кроме того, в Delphi реализована функция DebugConvert - отладочная конвертация,
  позволяющая сконвертировать недистиллированные запросы. в Java такого по-моему пока нет


13704		43717	 - Для полнотекстовых условий, требующих сортировку по релевантности,
		   сортировка не добавляется в том случае, если полнотекстовое условие содержится в подзапросе.

  то, что мы обсуждали. нужно реализовать функцию "чистки" запроса.

В java были портированы объект TPredicateFullTextSearch и его дистилляция и отражающие специфику oracle TOraFtsMarker, TOraFtsRange,

но не быда портирована функция "чистки" запроса, заменяющая TOraFtsMarker'ы на уникальные числовые метки и добавляющая необходимые 
сортировки.
Функция также отражает специфику oracle в части поддержки полнотекстовых условий и должна выполняться на дистиллированном запросе
непосредственно перед его исполнением.

  для реализации в Java - задача 44033.


========================================================
Не было портировано в Java, необходимо полностью реализовать:
- функцию объединения sqlobject-запросов;
- функцию "подъёма" параметров из запроса.


